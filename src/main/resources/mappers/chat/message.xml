<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.goottflix.chat.entity.mapper.MessageMapper">


    <insert id="insertMessage">
        INSERT INTO message(chat_room_id, sender_id, content)
        VALUES (#{chatRoomId}, #{senderId}, #{content})
    </insert>

    <select id="getMessagesByChatRoomId" resultType="MessageDTO">
        SELECT * FROM message WHERE chat_room_id = #{chatRoomId} ORDER BY send_at
    </select>


</mapper>